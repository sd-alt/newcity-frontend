# 请求超时与自动验证说明

前端 API 请求默认 30 秒超时，并支持通过 `timeoutMs` 为特殊接口延长时间：模型对话使用 120 秒，模型列表使用 20 秒，文件上传和文本导出使用 120 秒。调用方传入的 `AbortSignal` 仍然可以主动取消请求。

前端仓库的 GitHub Actions 会执行 `npm ci`、`npm run typecheck` 和 `npm run build`，用于提前发现接口类型和生产构建问题。
